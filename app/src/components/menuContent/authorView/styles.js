// src/components/menuContent/authorView/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  separatingContainerStyle: {
    marginTop: 32,
  },
  inputContainerStyle: {
    marginBottom: 12,
  },
});

export default styles;
