// src/components/common/sectionTitle/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 15,
    color: '#AAA',
  },
});

export default styles;
