// src/components/common/title/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  titleStyle: {
    fontSize: 20,
    fontWeight: '500',
    lineHeight: 24,
    backgroundColor: 'transparent',
  },
});

export default styles;
