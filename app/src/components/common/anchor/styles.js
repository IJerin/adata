// src/components/common/anchor/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  aStyle: {
    fontSize: 14,
    fontWeight: '600',
    lineHeight: 20,
    backgroundColor: 'transparent',
  },
});

export default styles;
