// src/components/landingPageView/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  contentContainerStyle: {
    position: 'relative',
  },
});

export default styles;
