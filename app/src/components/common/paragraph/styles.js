// src/components/common/paragraph/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  pStyle: {
    fontSize: 14,
    fontWeight: '400',
    lineHeight: 20,
    backgroundColor: 'transparent',
  },
});

export default styles;
