// src/components/common/clickableIcon/styles.ios.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  textStyle: {
    fontSize: 15,
    lineHeight: 24,
    textAlign: 'center',
  },
  roundedStyle: {
    borderColor: '#1BAC95',
    borderWidth: 1,
    overflow: 'hidden',
    justifyContent: 'center',
    alignItems: 'center',
  },
  roundedTextStyle: {
    fontSize: 14,
    color: '#1BAC95',
    textAlign: 'center',
  },
});

export default styles;
