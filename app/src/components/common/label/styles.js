// src/components/common/label/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  labelStyle: {
    fontSize: 18,
    fontWeight: '400',
    lineHeight: 22,
    backgroundColor: 'transparent',
  },
});

export default styles;
