// src/components/common/modalMenu/styles.js

import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  containerStyle: {
    width: '100%',
    backgroundColor: '#FFF',
    paddingVertical: 12,
    paddingHorizontal: 24,
  },
});

export default styles;
